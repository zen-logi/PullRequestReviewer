<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:PullRequestReviewer.ViewModels"
             xmlns:models="clr-namespace:PullRequestReviewer.Models"
             xmlns:converters="clr-namespace:PullRequestReviewer.Converters"
             x:Class="PullRequestReviewer.Views.PRListPage"
             x:DataType="viewmodels:PRListViewModel"
             Title="Pull Requests">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:FilterTypeToColorConverter x:Key="FilterTypeToColorConverter"/>
            <converters:FilterTypeToTextColorConverter x:Key="FilterTypeToTextColorConverter"/>
            <converters:StringNotEmptyConverter x:Key="StringNotEmptyConverter"/>
            <converters:InvertedBoolConverter x:Key="InvertedBoolConverter"/>
            <converters:BoolToFilterIconColorConverter x:Key="BoolToFilterIconColorConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Settings"
                     Command="{Binding NavigateToSettingsCommand}"/>
    </ContentPage.ToolbarItems>

    <Grid>
        <!-- Sidebar Toggle Button -->
        <Button x:Name="ToggleSidebarButton"
                Text="☰"
                FontSize="24"
                WidthRequest="50"
                HeightRequest="50"
                CornerRadius="25"
                BackgroundColor="{DynamicResource Primary}"
                TextColor="White"
                HorizontalOptions="Start"
                VerticalOptions="Start"
                Margin="10"
                Clicked="OnToggleSidebarClicked"
                ZIndex="1000"/>

        <!-- Main Content with Sidebar -->
        <Grid x:Name="MainGrid" ColumnDefinitions="Auto,*">

            <!-- Sidebar -->
            <Border x:Name="Sidebar"
                    Grid.Column="0"
                    BackgroundColor="{DynamicResource Gray100}"
                    StrokeThickness="0"
                    WidthRequest="200"
                    IsVisible="True"
                    ZIndex="999">
                <VerticalStackLayout Spacing="0" Padding="10,70,10,10">

                    <Border StrokeThickness="0"
                            StrokeShape="RoundRectangle 8"
                            Padding="15,12"
                            Margin="0,0,0,5">
                        <Border.BackgroundColor>
                            <MultiBinding Converter="{StaticResource FilterTypeToColorConverter}">
                                <Binding Path="CurrentFilter"/>
                                <Binding Source="All"/>
                            </MultiBinding>
                        </Border.BackgroundColor>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding FilterChangedCommand}"
                                                 CommandParameter="All"/>
                        </Border.GestureRecognizers>
                        <Label Text="All"
                               FontSize="16"
                               FontAttributes="Bold">
                            <Label.TextColor>
                                <MultiBinding Converter="{StaticResource FilterTypeToTextColorConverter}">
                                    <Binding Path="CurrentFilter"/>
                                    <Binding Source="All"/>
                                </MultiBinding>
                            </Label.TextColor>
                        </Label>
                    </Border>

                    <Border StrokeThickness="0"
                            StrokeShape="RoundRectangle 8"
                            Padding="15,12"
                            Margin="0,0,0,5">
                        <Border.BackgroundColor>
                            <MultiBinding Converter="{StaticResource FilterTypeToColorConverter}">
                                <Binding Path="CurrentFilter"/>
                                <Binding Source="ReviewRequested"/>
                            </MultiBinding>
                        </Border.BackgroundColor>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding FilterChangedCommand}"
                                                 CommandParameter="ReviewRequested"/>
                        </Border.GestureRecognizers>
                        <Label Text="Review Requested"
                               FontSize="16">
                            <Label.TextColor>
                                <MultiBinding Converter="{StaticResource FilterTypeToTextColorConverter}">
                                    <Binding Path="CurrentFilter"/>
                                    <Binding Source="ReviewRequested"/>
                                </MultiBinding>
                            </Label.TextColor>
                        </Label>
                    </Border>

                    <Border StrokeThickness="0"
                            StrokeShape="RoundRectangle 8"
                            Padding="15,12"
                            Margin="0,0,0,5">
                        <Border.BackgroundColor>
                            <MultiBinding Converter="{StaticResource FilterTypeToColorConverter}">
                                <Binding Path="CurrentFilter"/>
                                <Binding Source="Assigned"/>
                            </MultiBinding>
                        </Border.BackgroundColor>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding FilterChangedCommand}"
                                                 CommandParameter="Assigned"/>
                        </Border.GestureRecognizers>
                        <Label Text="Assigned"
                               FontSize="16">
                            <Label.TextColor>
                                <MultiBinding Converter="{StaticResource FilterTypeToTextColorConverter}">
                                    <Binding Path="CurrentFilter"/>
                                    <Binding Source="Assigned"/>
                                </MultiBinding>
                            </Label.TextColor>
                        </Label>
                    </Border>

                    <Border StrokeThickness="0"
                            StrokeShape="RoundRectangle 8"
                            Padding="15,12"
                            Margin="0,0,0,5">
                        <Border.BackgroundColor>
                            <MultiBinding Converter="{StaticResource FilterTypeToColorConverter}">
                                <Binding Path="CurrentFilter"/>
                                <Binding Source="Authored"/>
                            </MultiBinding>
                        </Border.BackgroundColor>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding FilterChangedCommand}"
                                                 CommandParameter="Authored"/>
                        </Border.GestureRecognizers>
                        <Label Text="Authored"
                               FontSize="16">
                            <Label.TextColor>
                                <MultiBinding Converter="{StaticResource FilterTypeToTextColorConverter}">
                                    <Binding Path="CurrentFilter"/>
                                    <Binding Source="Authored"/>
                                </MultiBinding>
                            </Label.TextColor>
                        </Label>
                    </Border>

                </VerticalStackLayout>
            </Border>

            <!-- Content Area -->
            <Grid Grid.Column="1">
                <RefreshView IsRefreshing="{Binding IsRefreshing}"
                            Command="{Binding RefreshCommand}">
                    <ScrollView>
                        <VerticalStackLayout>

                            <!-- Error Message -->
                            <Label Text="{Binding ErrorMessage}"
                                  IsVisible="{Binding HasError}"
                                  TextColor="Red"
                                  HorizontalOptions="Center"
                                  Margin="20"
                                  HorizontalTextAlignment="Center"/>

                            <!-- Pull Requests List -->
                            <CollectionView ItemsSource="{Binding FilteredPullRequests}"
                                          SelectionMode="None">
                                <CollectionView.EmptyView>
                                    <Label Text="No pull requests found"
                                          HorizontalOptions="Center"
                                          VerticalOptions="Center"
                                          Margin="0,50,0,0"
                                          TextColor="{DynamicResource Gray600}"
                                          FontSize="16"/>
                                </CollectionView.EmptyView>

                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="models:PullRequestModel">
                                        <Border Stroke="{DynamicResource Gray300}"
                                               StrokeThickness="1"
                                               Padding="15"
                                               Margin="10,5"
                                               BackgroundColor="White"
                                               StrokeShape="RoundRectangle 8">
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:PRListViewModel}}, Path=OpenPullRequestCommand}"
                                                    CommandParameter="{Binding .}"/>
                                            </Border.GestureRecognizers>

                                            <Grid RowDefinitions="Auto,Auto,Auto,Auto" ColumnDefinitions="Auto,*" RowSpacing="8">
                                                <!-- Avatar -->
                                                <Border Grid.Row="0" Grid.RowSpan="4" Grid.Column="0"
                                                       StrokeShape="RoundRectangle 20"
                                                       StrokeThickness="0"
                                                       WidthRequest="40"
                                                       HeightRequest="40"
                                                       Margin="0,0,12,0"
                                                       VerticalOptions="Start">
                                                    <Image Source="{Binding AuthorAvatarUrl}"
                                                          Aspect="AspectFill"/>
                                                </Border>

                                                <!-- Title and Status -->
                                                <HorizontalStackLayout Grid.Row="0" Grid.Column="1" Spacing="8">
                                                    <Label Text="{Binding Title}"
                                                          FontSize="16"
                                                          FontAttributes="Bold"
                                                          TextColor="{DynamicResource Gray900}"
                                                          VerticalOptions="Center"
                                                          LineBreakMode="WordWrap"
                                                          MaxLines="2"/>
                                                </HorizontalStackLayout>

                                                <!-- Repository Info -->
                                                <HorizontalStackLayout Grid.Row="1" Grid.Column="1" Spacing="5">
                                                    <Label Text="{Binding RepositoryFullName}"
                                                          FontSize="14"
                                                          TextColor="{DynamicResource Gray600}"/>
                                                    <Label Text="•"
                                                          FontSize="14"
                                                          TextColor="{DynamicResource Gray600}"/>
                                                    <Label Text="{Binding Number, StringFormat='#{0}'}"
                                                          FontSize="14"
                                                          TextColor="{DynamicResource Gray600}"/>
                                                </HorizontalStackLayout>

                                                <!-- Author and Date -->
                                                <HorizontalStackLayout Grid.Row="2" Grid.Column="1" Spacing="5">
                                                    <Label Text="{Binding AuthorLogin}"
                                                          FontSize="13"
                                                          TextColor="{DynamicResource Gray600}"/>
                                                    <Label Text="•"
                                                          FontSize="13"
                                                          TextColor="{DynamicResource Gray600}"/>
                                                    <Label Text="{Binding DisplayCreatedAt}"
                                                          FontSize="13"
                                                          TextColor="{DynamicResource Gray600}"/>
                                                </HorizontalStackLayout>

                                                <!-- State Badge -->
                                                <Border Grid.Row="3" Grid.Column="1"
                                                       BackgroundColor="{Binding StateColor}"
                                                       StrokeThickness="0"
                                                       Padding="8,4"
                                                       HorizontalOptions="Start"
                                                       StrokeShape="RoundRectangle 4">
                                                    <Label Text="{Binding State}"
                                                          TextColor="White"
                                                          FontSize="12"
                                                          FontAttributes="Bold"/>
                                                </Border>

                                                <!-- Review Status Badge -->
                                                <Border Grid.Row="3" Grid.Column="1"
                                                        BackgroundColor="{Binding ReviewStatusColor}"
                                                        StrokeThickness="0"
                                                        Padding="8,4"
                                                        HorizontalOptions="Start"
                                                        StrokeShape="RoundRectangle 4"
                                                        Margin="60,0,0,0"
                                                        IsVisible="{Binding ReviewStatus, Converter={StaticResource StringNotEmptyConverter}}">
                                                    <Label Text="{Binding ReviewStatus}"
                                                          TextColor="White"
                                                          FontSize="12"
                                                          FontAttributes="Bold"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </ScrollView>
                </RefreshView>
            </Grid>
        </Grid>

        <!-- FAB Container -->
        <HorizontalStackLayout HorizontalOptions="End"
                                VerticalOptions="End"
                                Spacing="12"
                                Margin="20"
                                ZIndex="1000">
            
            <!-- Filter Button - FAB Style -->
            <Border StrokeThickness="0"
                    StrokeShape="RoundRectangle 28"
                    WidthRequest="56"
                    HeightRequest="56"
                    BackgroundColor="{DynamicResource Primary}">
                <Border.Shadow>
                    <Shadow Brush="Black"
                            Offset="0,4"
                            Radius="8"
                            Opacity="0.3" />
                </Border.Shadow>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ToggleFilterPopupCommand}" />
                </Border.GestureRecognizers>
                <Label Text="🔽"
                       FontSize="24"
                       TextColor="{Binding IsStatusFilterActive, Converter={StaticResource BoolToFilterIconColorConverter}}"
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />
            </Border>
            
            <!-- Refresh Button - FAB Style -->
            <Border StrokeThickness="0"
                    StrokeShape="RoundRectangle 28"
                    WidthRequest="56"
                    HeightRequest="56"
                    BackgroundColor="{DynamicResource Primary}">
                <Border.Shadow>
                    <Shadow Brush="Black"
                            Offset="0,4"
                            Radius="8"
                            Opacity="0.3" />
                </Border.Shadow>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding RefreshCommand}" />
                </Border.GestureRecognizers>
                <Label Text="🔄"
                       FontSize="24"
                       TextColor="White"
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />
                <Border.Triggers>
                    <DataTrigger TargetType="Border" Binding="{Binding IsLoading}" Value="True">
                        <Setter Property="IsEnabled" Value="False" />
                        <Setter Property="Opacity" Value="0.5" />
                    </DataTrigger>
                </Border.Triggers>
            </Border>
        </HorizontalStackLayout>
        
        <!-- Filter Popup Overlay -->
        <Grid IsVisible="{Binding IsFilterPopupVisible}"
              BackgroundColor="#40000000"
              ZIndex="1500"
              HorizontalOptions="Fill"
              VerticalOptions="Fill"
              InputTransparent="False">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CloseFilterPopupCommand}" />
            </Grid.GestureRecognizers>
            
            <!-- Filter Popup Card -->
            <Border BackgroundColor="White"
                    StrokeShape="RoundRectangle 12"
                    StrokeThickness="0"
                    Padding="20"
                    WidthRequest="280"
                    HorizontalOptions="End"
                    VerticalOptions="End"
                    Margin="20,0,20,100">
                <Border.Shadow>
                    <Shadow Brush="Black"
                            Offset="0,4"
                            Radius="12"
                            Opacity="0.25" />
                </Border.Shadow>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer />
                </Border.GestureRecognizers>
                
                <VerticalStackLayout Spacing="12">
                    <Label Text="Filter by Status"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource Gray900}"/>
                    
                    <BoxView HeightRequest="1" 
                             BackgroundColor="{DynamicResource Gray200}"
                             Margin="0,4"/>
                    
                    <!-- Open Status -->
                    <Grid ColumnDefinitions="*,Auto">
                        <HorizontalStackLayout Spacing="8">
                            <Border BackgroundColor="#28a745"
                                    StrokeThickness="0"
                                    StrokeShape="RoundRectangle 4"
                                    Padding="8,4"
                                    VerticalOptions="Center">
                                <Label Text="Open"
                                       TextColor="White"
                                       FontSize="12"
                                       FontAttributes="Bold"/>
                            </Border>
                        </HorizontalStackLayout>
                        <CheckBox Grid.Column="1"
                                  IsChecked="{Binding CurrentStatusFilter.ShowOpen}"
                                  Color="{DynamicResource Primary}"
                                  VerticalOptions="Center"/>
                    </Grid>
                    
                    <!-- Closed Status -->
                    <Grid ColumnDefinitions="*,Auto">
                        <HorizontalStackLayout Spacing="8">
                            <Border BackgroundColor="#6f42c1"
                                    StrokeThickness="0"
                                    StrokeShape="RoundRectangle 4"
                                    Padding="8,4"
                                    VerticalOptions="Center">
                                <Label Text="Closed"
                                       TextColor="White"
                                       FontSize="12"
                                       FontAttributes="Bold"/>
                            </Border>
                        </HorizontalStackLayout>
                        <CheckBox Grid.Column="1"
                                  IsChecked="{Binding CurrentStatusFilter.ShowClosed}"
                                  Color="{DynamicResource Primary}"
                                  VerticalOptions="Center"/>
                    </Grid>
                    
                    <!-- Merged Status -->
                    <Grid ColumnDefinitions="*,Auto">
                        <HorizontalStackLayout Spacing="8">
                            <Border BackgroundColor="#6f42c1"
                                    StrokeThickness="0"
                                    StrokeShape="RoundRectangle 4"
                                    Padding="8,4"
                                    VerticalOptions="Center">
                                <Label Text="Merged"
                                       TextColor="White"
                                       FontSize="12"
                                       FontAttributes="Bold"/>
                            </Border>
                        </HorizontalStackLayout>
                        <CheckBox Grid.Column="1"
                                  IsChecked="{Binding CurrentStatusFilter.ShowMerged}"
                                  Color="{DynamicResource Primary}"
                                  VerticalOptions="Center"/>
                    </Grid>
                    
                    <!-- Draft Status -->
                    <Grid ColumnDefinitions="*,Auto">
                        <HorizontalStackLayout Spacing="8">
                            <Border BackgroundColor="#6c757d"
                                    StrokeThickness="0"
                                    StrokeShape="RoundRectangle 4"
                                    Padding="8,4"
                                    VerticalOptions="Center">
                                <Label Text="Draft"
                                       TextColor="White"
                                       FontSize="12"
                                       FontAttributes="Bold"/>
                            </Border>
                        </HorizontalStackLayout>
                        <CheckBox Grid.Column="1"
                                  IsChecked="{Binding CurrentStatusFilter.ShowDraft}"
                                  Color="{DynamicResource Primary}"
                                  VerticalOptions="Center"/>
                    </Grid>
                    
                    <BoxView HeightRequest="1" 
                             BackgroundColor="{DynamicResource Gray200}"
                             Margin="0,4"/>
                    
                    <!-- Reset Button -->
                    <Button Text="Reset Filter"
                            Command="{Binding ResetStatusFilterCommand}"
                            BackgroundColor="{DynamicResource Gray200}"
                            TextColor="{DynamicResource Gray900}"
                            FontAttributes="Bold"
                            CornerRadius="8"
                            HeightRequest="44"/>
                </VerticalStackLayout>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Grid IsVisible="{Binding IsLoading}"
              BackgroundColor="#40000000"
              ZIndex="2000"
              HorizontalOptions="Fill"
              VerticalOptions="Fill"
              InputTransparent="False">
            <Border BackgroundColor="White"
                    StrokeShape="RoundRectangle 10"
                    Padding="20"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                <VerticalStackLayout Spacing="10">
                    <ActivityIndicator IsRunning="{Binding IsLoading}"
                                      Color="{DynamicResource Primary}"
                                      WidthRequest="50"
                                      HeightRequest="50"/>
                    <Label Text="Loading..."
                           TextColor="{DynamicResource Gray900}"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Border>
        </Grid>

    </Grid>

</ContentPage>
